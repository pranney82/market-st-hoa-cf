---
import Base from "./Base.astro";
import InstallPrompt from "../components/InstallPrompt";
import PushOptIn from "../components/PushOptIn";
import ThemeToggle from "../components/ThemeToggle.astro";

interface Props {
  title: string;
}
const { title } = Astro.props;
const user = Astro.locals.user!;
const path = Astro.url.pathname;

const nav = [
  { href: "/dashboard", label: "Dashboard", icon: "home" },
  { href: "/payments", label: "Payments", icon: "credit-card" },
  { href: "/announcements", label: "Announcements", icon: "megaphone" },
  { href: "/documents", label: "Documents", icon: "file-text" },
  { href: "/requests", label: "Requests", icon: "clipboard" },
  { href: "/bylaws", label: "Bylaws", icon: "book" },
  { href: "/voting", label: "Voting", icon: "vote" },
  { href: "/members", label: "Members", icon: "users" },
];

const adminNav = [
  { href: "/households", label: "Households", icon: "building" },
  { href: "/financials", label: "Financials", icon: "bar-chart" },
  { href: "/admin/accounts", label: "Accounts", icon: "layers" },
  { href: "/admin/users", label: "Users", icon: "shield" },
];

const isAdmin = user.role === "admin" || user.role === "board";
---
<Base title={title} class="bg-gray-50 dark:bg-gray-950 min-h-screen">
  <div class="flex min-h-screen">
    <!-- Sidebar -->
    <aside class="w-64 bg-white dark:bg-gray-900 border-r border-gray-200 dark:border-gray-700 flex flex-col shrink-0 hidden md:flex">
      <div class="p-4 border-b border-gray-200 dark:border-gray-700">
        <div class="flex items-center gap-2">
          <div class="w-8 h-8 bg-blue-600 rounded-md flex items-center justify-center text-white text-sm font-bold">M</div>
          <span class="font-semibold text-sm dark:text-white">Market St HOA</span>
          <div class="ml-auto">
            <ThemeToggle />
          </div>
        </div>
      </div>

      <nav class="flex-1 p-3 space-y-1">
        {nav.map(({ href, label }) => (
          <a
            href={href}
            class:list={[
              "block px-3 py-2 rounded-md text-sm transition-colors",
              path === href ? "bg-blue-50 dark:bg-blue-900/30 text-blue-700 dark:text-blue-300 font-medium" : "text-gray-600 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-800"
            ]}
          >
            {label}
          </a>
        ))}

        {isAdmin && (
          <>
            <div class="pt-4 pb-1 px-3 text-xs font-medium text-gray-400 dark:text-gray-500 uppercase">Admin</div>
            {adminNav.map(({ href, label }) => (
              <a
                href={href}
                class:list={[
                  "block px-3 py-2 rounded-md text-sm transition-colors",
                  path === href ? "bg-blue-50 dark:bg-blue-900/30 text-blue-700 dark:text-blue-300 font-medium" : "text-gray-600 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-800"
                ]}
              >
                {label}
              </a>
            ))}
          </>
        )}
      </nav>

      <div class="p-4 border-t border-gray-200 dark:border-gray-700">
        <div class="flex items-center gap-3">
          <div class="w-8 h-8 bg-gray-200 dark:bg-gray-700 rounded-full flex items-center justify-center text-xs font-medium text-gray-600 dark:text-gray-300">
            {user.firstName?.[0]}{user.lastName?.[0]}
          </div>
          <div class="flex-1 min-w-0">
            <p class="text-sm font-medium text-gray-900 dark:text-white truncate">{user.firstName} {user.lastName}</p>
            <p class="text-xs text-gray-500 dark:text-gray-400 truncate">{user.email}</p>
          </div>
        </div>
        <a href="/settings" class="block mt-3 text-xs text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:hover:text-gray-300">Settings</a>
        <PushOptIn client:idle />
        <form method="POST" action="/api/logout" class="mt-1">
          <button type="submit" class="text-xs text-red-500 hover:text-red-700">Sign out</button>
        </form>
      </div>
    </aside>

    <!-- Main content -->
    <main class="flex-1 p-6 overflow-y-auto">
      <slot />
    </main>
  </div>

  <!-- PWA install prompt (React island, loads after idle) -->
  <InstallPrompt client:idle />
</Base>
